# Examples of Good PR Titles

```
Hide vim cursor(s) when in command mode
Properly focus (and unfocus) multi-cell delete confirmation
Fix `Enter` key handling for delete confirmation button
Add additional vim keybindings for cell operations
Extend destructive delete behavior to multi-select cells
Update multi-cell toolbar labels: "Hide/Show cells" → "Hide/Show code"
Add vim bindings for top/bottom cell navigation: `gg` and `G`
Propagate `Ctrl+[` as `Escape` to enter command mode with vim keybindings
Fix handling of class attribute references for reactive variables
Add quick actions item to change cell output area
Elevate `preferObjectParams` linting severity from "warn" to "error"
Enable TypeScript incremental compilation
Add linting rule to prefer options object over multiple positional args
Make reactivity options in footer responsive for small screen widths
Add breaking changes section to release notes generation
Pin Python version upper bound for `scripts/generate_release_notes.py`
Don't highlight keyword argument names as reactive references
Convert import helpers to use named parameters
Ensure cell action tooltip appears above sidebar panel
Restore DOM-based metadata for VS Code extension compatibility
Add Vim-style autocompletion keybindings (Ctrl-y, Ctrl-n, Ctrl-p)
```
